# Релиз и описание продукта

## Область применения

В этом документе описываются единый вход в выпуск, список продуктов выпуска, параметры ручного запуска и общие пути устранения неполадок текущего склада.

## Единый вход в издательство

На данный момент единственный рабочий процесс публикации:

- `.github/workflows/release-all.yml`
- Интерфейс `dist` будет сначала создаваться отдельно, а затем распространяться на каждую платформу для упаковки и повторного использования заданий.

Метод триггера:

- Поддерживает только `workflow_dispatch`
- По умолчанию он не будет автоматически запускаться с помощью запроса push/pull.

Основной ввод:

- `tag`: Требуется тег выпуска.
- `ref`: создать базовую ветку или зафиксировать, по умолчанию `main`.
- `prerelease`: `auto | true | false`
- Примечание. Рабочий процесс отвечает только за упаковку и публикацию и больше не включает шлюзы тестирования на стороне сервера.

## Список продуктов

### Рабочий стол

- Windows: `CodexManager_<version>_x64-setup.exe`
- Windows:`CodexManager-portable.exe`
- macOS: `CodexManager_<version>_aarch64.dmg`
- macOS: `CodexManager_<version>_x64.dmg`
- Linux: `CodexManager_<version>_amd64.AppImage`
- Linux: `CodexManager_<version>_amd64.deb`
- Linux:`CodexManager-linux-portable.zip`

### Service

- Windows:`CodexManager-service-windows-x86_64.zip`
- macOS:`CodexManager-service-macos-arm64.zip`
- macOS:`CodexManager-service-macos-x64.zip`
- Linux:`CodexManager-service-linux-x86_64.zip`
- Linux (пакет веб-тестирования): `CodexManager-web-linux-x86_64.zip`

### GitHub Вложение по умолчанию

GitHub Релиз по-прежнему будет автоматически включать:

- `Source code (zip)`
- `Source code (tar.gz)`

## pre-release Правила

- `prerelease=auto` и `tag` содержат `-`: опубликовано как pre-release.
- `prerelease=auto` и `tag` не содержат `-`: выпущено как официальная версия.
- `prerelease=true|false`: Принудительная отмена автоматического решения
- При повторном запуске того же `tag` метаданные Release будут повторно синхронизированы на основе этих входных данных.

## Локальная запись триггера

Windows Локальный вспомогательный скрипт:

- `scripts/rebuild.ps1`

Общее использование:

```powershell
pwsh -NoLogo -NoProfile -File scripts/rebuild.ps1 `
  -AllPlatforms `
  -GitRef main `
  -ReleaseTag v0.1.9 `
  -GithubToken <token>
```

Часто используемые параметры:

- `-AllPlatforms`: Триггер `release-all.yml`
- `-ReleaseTag`: Опубликовать тег
- `-GitRef`: ссылка на сборку
- `-Prerelease`: явно указать статус pre-release.
- `-DownloadArtifacts`: Загружать ли артефакты сборки после запуска

## Описание различий платформы

### Windows

- Одновременно создайте установочную и портативную версии.
- Портативная версия в настоящее время представляет собой единый `exe`, и дополнительный уровень zip не требуется.

### macOS

- Текущий продукт — `dmg`.
- Поскольку учетная запись Apple Developer не заверена нотариально, первый запуск все равно может быть перехвачен Gatekeeper.
- `dmg` Он будет включать в себя:
  - `Open CodexManager.command`
  - `README-macOS-first-launch.txt`

### Linux

- В настоящее время на настольном компьютере доступны `AppImage` и `deb`.
- Service Версия выпущена в виде сжатого пакета.

## Рекомендуется проверить перед выпуском

1. Подтвердите, что номер версии синхронизирован через `scripts/bump-version.ps1`.
2. Подтвердите, что `CHANGELOG.md` обновлено
3. Подтвердите, что сборка интерфейса для настольных компьютеров прошла успешно: `pnpm -C apps run build`.
4. Подтвердите прохождение основных тестов: `pnpm -C apps run test`, `cargo test --workspace`.
5. Если требуются изменения в протоколе шлюза, выполните дополнительные `scripts/tests/gateway_regression_suite.ps1`.

## Устранение распространенных неисправностей

### Отсутствуют артефакты интерфейса

Приоритетные проверки:

- `apps/out/` Можно ли его построить нормально
- Успешно ли завершен этап сборки интерфейса в рабочем процессе.

### Метаданные выпуска неверны.

Приоритетные проверки:

- `tag` Содержит ли он `-`
- `prerelease` Является ли автоматическое суждение явно отмененным

### macOS Продукт можно загрузить, но нельзя открыть в первый раз.

Это ожидаемое ограничение в текущем ненотаризованном состоянии и не является сбоем сборки рабочего процесса.

Метод обработки:

1. Сначала перетащите `CodexManager.app` в «Приложения».
2. Дважды щелкните `Open CodexManager.command`
3. или выполните:

```bash
xattr -dr com.apple.quarantine /Applications/CodexManager.app
```

## Сопутствующие документы

- Корневое описание: [README.md](../README.md)
- Английское описание: [README.en.md](../README.md)
- Базовый уровень теста: [TESTING.md](../TESTING.md)
- Описание архитектуры: [ARCHITECTURE.md](../ARCHITECTURE.md)